# 贡献指南

感谢你考虑为代码统计工具做出贡献！🎉

## 如何贡献

### 报告问题

如果你发现了 bug 或有功能建议：

1. 检查 [Issues](https://github.com/your-username/collect-code/issues) 确保问题未被报告
2. 创建新的 Issue，包含：
   - 清晰的标题
   - 详细的描述
   - 复现步骤（如果是 bug）
   - 期望的行为
   - 实际的行为
   - 截图（如果适用）

### 提交代码

1. **Fork 项目**
   ```bash
   # 点击 GitHub 页面上的 Fork 按钮
   ```

2. **克隆你的 Fork**
   ```bash
   git clone https://github.com/your-username/collect-code.git
   cd collect-code
   ```

3. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

4. **安装依赖**
   ```bash
   # 前端
   cd frontend && npm install
   
   # 后端
   cd ../backend && npm install
   ```

5. **进行修改**
   - 遵循现有代码风格
   - 添加必要的注释
   - 确保代码可读性

6. **测试你的修改**
   ```bash
   # 启动后端
   cd backend && npm start
   
   # 启动前端
   cd frontend && npm run dev
   ```

7. **提交修改**
   ```bash
   git add .
   git commit -m "feat: 添加新功能描述"
   # 或
   git commit -m "fix: 修复某个问题"
   ```

8. **推送到你的 Fork**
   ```bash
   git push origin feature/your-feature-name
   ```

9. **创建 Pull Request**
   - 访问原项目的 GitHub 页面
   - 点击 "New Pull Request"
   - 选择你的分支
   - 填写 PR 描述

## 提交信息规范

使用语义化的提交信息：

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具相关

示例：
```
feat: 添加分支统计功能
fix: 修复文件夹选择路径问题
docs: 更新 README 使用说明
```

## 代码规范

### JavaScript/React

- 使用 2 空格缩进
- 使用单引号
- 组件名使用 PascalCase
- 函数名使用 camelCase
- 常量使用 UPPER_CASE

### Python

- 遵循 PEP 8 规范
- 使用 4 空格缩进
- 函数名使用 snake_case

### CSS

- 使用有意义的类名
- 遵循 BEM 命名规范（可选）
- 保持样式的可读性

## 开发建议

### 前端开发

```bash
cd frontend
npm run dev  # 开发服务器自动刷新
```

### 后端开发

```bash
cd backend
npm run dev  # 使用 nodemon 自动重启
```

### Python 脚本

```bash
python3 scripts/git_stats.py ~/test-projects
```

## 功能请求

如果你有好的想法：

1. 先创建 Issue 讨论
2. 等待维护者反馈
3. 获得批准后再开始开发

## 需要帮助的领域

- 🌐 国际化支持
- 📊 数据可视化（图表）
- 🎨 UI/UX 改进
- 📱 移动端适配
- 🧪 测试覆盖
- 📝 文档完善

## 行为准则

- 尊重所有贡献者
- 接受建设性批评
- 专注于对项目最有利的方面
- 保持友好和专业

## 许可证

贡献代码即表示你同意在 MIT 许可证下发布你的代码。

## 联系方式

- 提 Issue: [GitHub Issues](https://github.com/your-username/collect-code/issues)
- 讨论: [GitHub Discussions](https://github.com/your-username/collect-code/discussions)

感谢你的贡献！🙏

